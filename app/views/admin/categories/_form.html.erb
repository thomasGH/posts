<%= form_for([:admin, @category]) do |f| %>
  <%= render 'common/errors', object: @category %>  

  <div class="field">
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
