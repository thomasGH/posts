<%= form_for(@category) do |f| %>
  <%= render 'common/errors', object: @category %>  

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
