<%= form_for([@post, @comment]) do |f| %>
  <%= render 'common/errors', object: @comment %>

  <div class="field">
    <%= f.label "Ваш комментарий" %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
